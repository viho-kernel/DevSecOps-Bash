==================DAY-7=======================
User Creation Automation:
Creating a user
setting home directory
giving a random password to it
asking user to change it.

/etc/passwd ==> to check users
useradd -m vihari ==> to add user
grep -i ==> to match relative words
grep -w ==> to match exact word

$# == no. of arguments.
$@ == all arugments.
$RANDOM == will generate a randome function.

fold -w1 ==> will break a horizontal sequence to a vertical sequence.
fold -w1 | shuf ==> if you do this it will shuffle the horizontal code randomly
head -1 | will take top character
tail -1 | will take bottom character.

to replace a text
Translate -tr == to change a spectific character
Stream Editor -sed == to change whole sequence in a line. 